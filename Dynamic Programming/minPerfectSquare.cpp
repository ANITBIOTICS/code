#include <bits/stdc++.h>
using namespace std;

int recur(int n){
    if(n == 0){
    return 0;
    }

    int count = n;

    for(int i = 0; i * i < n; i++){
        
    }
    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr); 
    int n;
    cin >> n;
    cout << recur(n);
    return 0;
}