#include <bits/stdc++.h>
using namespace std;

int trig(int n){
    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    cout << trig(n);
    return 0;
}