#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr); 
    string original; cin >> original;
    string later; cin >> later;
    if(later.size() > original.size()){
        cout << "N";
    }else{
        vector<int> numAlphabets;
    }
    return 0;
}