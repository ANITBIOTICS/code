#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr); 

    int N;
    cin >> N;

    /*
    let the theoretical number of things said be M.
    M = N + M/5 + M/3 - M%15
    
    */

    return 0;
}